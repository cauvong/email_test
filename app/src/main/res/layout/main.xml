<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:id="@+id/main_layout"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent"
                android:paddingLeft="16dp"
                android:paddingRight="16dp" 
                android:orientation="vertical"
>

    <Button
        android:id="@+id/main_mail"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:text="@string/send_mail" 
        android:onClick="email" 
    />
    
    <Button
        android:id="@+id/main_inbox"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/main_mail"
        android:text="Inbox" 
        android:onClick="inbox" 
    />
    
    <Button
        android:id="@+id/main_contact"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/main_inbox"
        android:text="Nieuw contact toevoegen" 
        android:onClick="contacts" 
    />
    
    <Button
        android:id="@+id/main_settings"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/main_contact"
        android:text="Settings" 
        android:onClick="settings" 
    />
    
</LinearLayout>

